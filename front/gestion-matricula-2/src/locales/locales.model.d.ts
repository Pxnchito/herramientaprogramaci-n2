export interface LocalesDTO{
    codLocal: Integer;
    direccion: string;
}

export interface LocalesRegistrarDTO{
    direccion: string;
}
export interface LocalesBorrarDTO{
    codLocal: Integer;
}